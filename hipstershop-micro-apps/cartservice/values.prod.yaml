# ─────────────────────────────────────────────────────────────
#  cartservice — prod overrides
# ─────────────────────────────────────────────────────────────
global:
  replicas:
    cartservice: 1
  resources:
    standard:
      requests: { cpu: 100m, memory: 128Mi }
      limits:   { cpu: 200m, memory: 256Mi }
  images:
    cartservice: "yampss/hipstershop-cartservice:latest"
