# This file is generated by generate-sealed-secrets.sh
# Run the script after the sealed-secrets controller is installed.
# DO NOT edit manually — re-run the script if values change.
#
# Secrets managed here (hipster-database namespace):
#   - mongodb-root      (MONGO_ROOT_USERNAME, MONGO_ROOT_PASSWORD)
#   - mongodb-users     (per-service username/password pairs)
#
# STATUS: placeholder — replace this file by running generate-sealed-secrets.sh
